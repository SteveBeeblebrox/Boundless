{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "Boundless",
  "description": "TODO",
  "authors": [
    "TODO"
  ],
  "contact": {
    "website": "TODO",
    "repo": "TODO"
  },
  "license": "MIT",
  "icon": "assets/${id}/icon.png",
  "environment": "*",
  "entrypoints": {
    "client": [
      "${pkgroot}.${id}.client.BoundlessClient"
    ],
    "main": [
      "${pkgroot}.${id}.Boundless"
    ]
  },
  "mixins": [
    "${id}.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.14.21",
    "minecraft": "1.20-beta.6"
  }
}
